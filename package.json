{
  "name": "research-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check:content": "node scripts/check-shared-content.mjs",
    "test": "node --test",
    "test:init-integration": "node --test tests/init-integration.test.mjs",
    "verify": "npm run check:content && npm test && npm run test:init-integration"
  }
}
